#include<bits/stdc++.h>
using namespace std;
int main(){
    int n, m; 
    cout<<"enter size of first array: ";
    cin>>n;
        cout<<"enter size of first array: ";
        cin>>m;
    int arr[n], arr2[m];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    for(int i=0;i<m;i++)
    {
        cin>>arr2[i];
    }
    int arr3[100];
    arr3=arr+ arr2;
    for(int )
}